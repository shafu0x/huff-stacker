/* Methods */
#define macro SET_VALUE() = takes (0) returns (0) {
    0x04 
    calldataload   
    [VALUE_LOCATION]   
    sstore            
}

/* Methods */
//#define macro SET_VALUE() = takes (0) returns (0) {
    0x04 calldataload   // [value]
    [VALUE_LOCATION]    // [ptr, value]
    sstore              // []
}
